// API_URL
// !!!Do not replace API_URL default value when build docker
// The start.zh will replace the default value with the environment variable API_URL,
// in order to use the docker environment variable to change the backed url
export const API_URL = "ws://127.0.0.1:8000";
